# Next 프록시가 /api/* 를 전달할 백엔드 주소 (런타임 server 변수, NEXT_PUBLIC_ 아님)
#
# 로컬 개발(pnpm dev): 백엔드 IP:PORT
BACKEND_URL=http://175.106.96.71:8081
#
# Docker 배포(docker compose): host.docker.internal 이 호스트 머신을 가리킴
# Linux/Mac/Windows 공통 — extra_hosts 설정으로 자동 주입됨
# BACKEND_URL=http://host.docker.internal:8080
